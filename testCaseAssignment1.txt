@isTest
private class TestUpdateAccountField {
    static testMethod void tsAccount(){
        List<Account> lac = new List<account>();
        set<Id> setids =new set<Id>();
        Map<Id,Opportunity> mapOpp =new Map<Id,Opportunity>();
opportunity opp = new opportunity();
opp.name='test opp';
opp.stagename='closed won';
        opp.Amount=53000;
        opp.CloseDate=Date.newInstance(2021,11,20);
        opp.Name = 'updating Account field to true';
        opp.AccountId = opp.ID;
        test.startTest();
        insert opp;
         test.stopTest();
        for( Account acc :[select id,is_goldenAccount__c from Account Where Id IN :setids]){
            if(mapOpp.get(opp.Id).Amount  > 50000)
        {
      acc.is_goldenAccount__c= TRUE;
      }
       else
       {
     acc.is_goldenAccount__c= FALSE;
       }
         }
        }
      }

